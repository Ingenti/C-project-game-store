#include "driver.hpp"

/*
Driver::Driver(Target starting_node, std::vector< std::pair<Target, int> > targets, std::vector<Road> path, Road previous_road) {
	starting_node_ = starting_node;
	targets_ = targets;
	path_ = path;
	previous_road_ = previous_road;
}
*/

Driver::Driver(Target starting_node, std::vector< std::pair<Target, int> > targets);	


void Driver::Update() {}



